services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # Persist the SQLite database across restarts
      - ./data:/app/data
    environment:
      DATABASE_URL: sqlite+aiosqlite:///data/plurality.db
